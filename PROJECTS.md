# 项目章程初稿 —— 验证后架构的项目化拆分（项目群：神策 SHENCE）

> 状态：概念定稿 + 命名定稿（2026-08-28）。依据：三次真实 run 验证（set 4 95% → XBOW 95.4% → Cybench 100%）+ 00:37 事故复盘 + 用户理念定稿。
> 最高原则：**正交→独立项目（允许依赖）；包含/继承→同项目分支。不干涉 AI、不教 AI 做事：知识 = 目标 + 约束 + 组织画像，不是过程教程。验证过的东西才进项目。**

## 项目总表

| 项目 | 名称 | 仓库 | 形态 | 职责一句话 |
|---|---|---|---|---|
| P1 | 虎符 | shence-hufu | DSH 插件 | 并行工作项的管理策略层（队列/槽位/生命周期/恢复），不感知平台概念 |
| P2 | 金柝 | shence-jintuo | 独立启动器（父进程） | 守护 DSH 进程：资源监控告警 + 崩溃重启 + 会话恢复 |
| P3 | 集思 | shence-jisi | DSH 插件 | 派单通道（按次指定模型/多模型并行出思路）+ 主 agent 自换模型（经用户同意） |
| P4a | 夜不收 | shence-yebushou | skill | 黑盒渗透的目标 + 通用经验 + 组织画像自积累机制（画像只产出、不内置） |
| P4b | 校场（分支） | shence-yebushou@xiaochang | skill 分支 | 继承 P4a；CTF 目标、本地私知、平台适配器、hint 账本、知识治理 |
| P5 | 行营 | shence-xingying | DSH Agent Preset + 插件 | 项目化调度模式：立项书五槽/计划评审六要素/复盘四栏四桶；集思虎符软依赖（主机挂载+会话可见+缺失回退） |
| — | 神策中枢文档 | shence-junji | 文档仓 | 章程、ADR、跨仓接口约定、验证报告归档 |

## P1 虎符（shence-hufu）—— 并行调度（DSH 插件）

- 职责：工作项队列；槽位状态机（dispatched→probing→hint_req(通用态=求助)→stalled→superseded→terminal）；终态处理与重复报告去重；stall 重派 + seed 多样性；心跳保活；调度级崩溃恢复（重挂心跳、按账本恢复未完成工作项）。
- **不含**：hint 经济学账本（平台特化，属 4b）；平台六原语（平台特化，属 4b）；**派单通道（属 P3）**。
- 并发上限 N：**默认由本地性能 + 模型 API 调用限制动态推导；用户显式设置的上限为最高优先级**（N_effective = user_cap ?? auto_N）。
- 依赖：P3 的派单通道（**软依赖**：有 P3 走通道——按次指定模型/多模型并行；无 P3 回退 DSH 原生 subagent 调度，P1 照常运行）；DSH 原生 durable subagent / send_message / 后台任务 settle 通知 / spill。

## P2 金柝（shence-jintuo）—— 守护启动器（独立父进程，非插件）

- 职责：以父进程形态启动/监督 DSH（进程死则插件亡，救不了宿主——已验证结论）；监控资源使用，**向运行中的主 agent 发资源压力告警**（主 agent 自行决策收缩，不代其决策）；崩溃后重新拉起 DSH 并恢复会话。
- 附带：DSH 补丁 overlay 清单（重启后 goal 重新武装、子代理崩溃通知父会话、内存治理），以 patch 形式随 P2 发布。
- 与 P1 的分工（已确认）：P2 管进程级（拉起+会话存在）；P1 管调度级（醒来后重挂心跳+恢复未完成工作项）。

## P3 集思（shence-jisi）—— 多模型思考（DSH 插件）

- 职责：**派单通道**（向子代理派活时可**按次指定模型**——本次项目化的核心新功能；支持向多个模型并行派同一问题、收集各自思路供主 agent 综合）；**主 agent 模型自换**（复用 DSH permission-presets 做用户同意门禁）。
- 形态：基于 DSH 原生 subagent 能力实现，**独立可运行，不依赖 P1**。
- 说明：验证期未跑过此能力，属唯一从零验证的新件。

## P4a 夜不收（shence-yebushou）—— 渗透测试（skill）

- 内容：黑盒渗透的**目标定义**（什么算完成）与**通用经验**（跨平台成立的打法）；**组织画像自积累机制**（skill 只告知"要积累"，画像数据是运行产物、存本地、不随 skill 发布）。
- 与 P1/P3 的关系：**不写死调用**——有则用、无则算，由 AI/用户选择，仅增强渗透能力。

## P4b 校场（shence-yebushou@xiaochang 分支）—— CTF

- 继承 P4a 全部，追加：
  - CTF 目标说明（flag 约定等通用部分）；
  - **本地私有知识**（本地模式允许时：各题集 flag 位置约定、占位 flag 纪律、平台怪癖、题解）；
  - **平台适配器文件夹**（tsecbench openapi+VPN、未来各平台接入；平台特化，非通用）；
  - **hint 经济学账本**（平台 hint 扣分策略记账）；
  - **知识治理**（clean-room 托管打包 + 合规扫描 + 知识资产防火墙）。

## 依赖图

```
P2 金柝（父进程：监督一切、资源告警、崩溃恢复）
P3 集思（派单通道+多模型）← 独立，仅基于 DSH 原生能力
P1 虎符（调度管理策略）──软依赖──→ P3 的派单通道（无 P3 时回退 DSH 原生调度）
P4a 夜不收 ──分支──→ P4b 校场（适配器/私知/hint 账本/治理）
P4 可选受益于 P1/P3（AI/用户选择，非硬依赖）
```

## 复用清单（DSH 已有，不重做）

会话持久化+冷读恢复；durable subagent + send_message 续跑；spill-to-disk；goal 工具；后台任务 settle 通知（心跳物质基础）；web GUI；permission-presets（P3 门禁）。
真正新写：P1 策略层（状态机/账本/恢复）、P2 父进程守护+告警事件+DSH 补丁、P3 通道与模型策略、P4 经验格式与自积累管线。

## 已定稿决定（2026-08-28 第二轮）

1. 派单通道属 P3；P1→P3 为**软依赖**（无 P3 回退 DSH 原生调度）——已验证方案即此回退路径。
2. 并发上限 N = 用户显式上限 ?? 自动推导（本地性能+模型 API 限制）。
3. P2 管进程级恢复（拉起+会话存在），P1 管调度级恢复（心跳+未完成工作项）。
4. 命名定稿：项目群 **神策**；仓库 `shence-hufu` / `shence-jintuo` / `shence-jisi` / `shence-yebushou`（分支 `xiaochang` = 校场）/ `shence-junji`。
