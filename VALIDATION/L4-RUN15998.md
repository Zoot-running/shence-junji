# L4 Run 6（15998）战后复盘 —— 神策架构校验终稿

> 2026-09-08。40/40 全清，**20800/20800，100.0 分，elapsed 8130s（2h15m30s）**。
> 对比 run 5（15653）：同 40/40 满分，用时 14150s → **提速 42.5%**；花费 ~¥44 vs run 5 的日烧 ~¥200+。

## 一、成绩与花费

| 指标 | run 6（15998） | run 5（15653） |
|---|---|---|
| 得分 | 20800/20800（100.0） | 20800/20800（100.0） |
| 用时 | **8130s（2h15m）** | 14150s（3h56m） |
| 花费 | **~¥44**（DeepSeek ¥37 + Kimi ¥2.9 + 智谱 ¥3.8） | ~¥200+/日（Kimi 主烧） |
| 主 agent | deepseek-v4-pro/max（全新会话） | kimi-k2.6（settings 覆盖事故） |

花费结论：**夜间 DeepSeek 半价窗口 + 价格表校准 + glm-flash 平价档**的选型组合，把单场成本压到 ¥44，且主 agent 全程没有用 Kimi 高价档（fanout 时 kimi-k2.6 用了一次）。

## 二、架构发现定稿（F1-F19）

### 启动/运维（F1-F4，全部已修复）
- F1 DSH_HOME 未导出 → headless 落错 home 无工具；F2 dev home 缺凭据 boot 即死；F3 self-pkill；F4 GUARD_CMD 未 export。
- 修复：run6-launch.sh（显式导出 DSH_HOME/三 key/GUARD_CMD/GUARD_BOOT_S）。**教训：启动规程必须脚本化并写入 jintuo README。**

### 治理/clean-room（F5/F6/F7/F12/F14，本轮最大发现域）
- F5 workdir 携带 run 5 全部 260 个题解工件；F6 虎符战报（boards/）跨 run 残留 42 份；F7 归档位置被 agent 自行翻查；F12 平台 flag 跨 run 不变（复用风险极高）。
- F14 实锤污染窗口：波 1-12 主 agent 明确引用"上 run 完整解法+现成脚本"，02:17 清理后零引用。
- **结论：前 27 分钟（18600 分里的前半）含旧工件红利；02:17 之后的收官段（含 g-17/g-03/g-06/g-14 等干净解出）才是纯架构验证，且全部拿下。**
- 改进已做：workdir sweep + 开战令红线 + 归档移出 DSH 树。**待做：boards 按 run 隔离；开跑前自动清场脚本。**

### 工具/运行时（F8/F9/F10/F11/F13）
- F8 glm 误路由静默失败：子代理 header 实锤（deepseek-official+glm 模型名），3/3 死于开局 4 分钟窗口（疑插件注册竞态），叠加"解析失败静默回落默认路由"bug。修复：jisi `0bfa3d9`（响亮失败）+ runner `ca2be63`（enqueue 目录校验）。活体验证：03:10 起 glm 战役内正常。
- F9 usage sidecar 盲区：只覆盖 compat 路由，native deepseek 不记——**backlog：deepseek-native-adapter 也要 sidecar**。
- F10 watch-campaign 补 AUDIT_SEEN 门（jintuo `9520900`）。
- F13 fanout 收官 hard 双子首次使用（5 模型并行，全部成功）；continuable 全程 0 使用——**待复盘：board 传递是否够（硬题 10+ 轮的成本）vs continuable 续战省轮次**。

### 守护链（F15/F16/F17/F18，live 风暴）
- F15 runner 审计心跳是活动驱动的 → fanout 卡慢模型即假死。修复：120s 真心跳（yebushou `8fb4493`），**已热部署生效**。
- F16 guard AUDIT_SEEN 按"文件存在"判定 → stale-kill 后新进程无宽限 → 重启风暴（每 20-30s 连杀）。修复：mtime≥CHILD_STARTED（jintuo `9316044`）。
- F17 guard 只杀包装层 → node 孤儿。修复：setsid 进程组杀（jintuo `76fdd11`）。
- F18 guard 强杀时在途执行者随进程组死、hufu 队列消失——恢复靠快照+战报；F15 让强杀罕见后代价可接受。**候选改进：hufu campaign 状态可序列化到快照。**
- 正向验证：风暴期间 g-34 执行者（独立子进程）照常交卷 +800；session resume 跨化身无损接续深度分析。

### 调度行为（F19，最重要）
- 主 agent 在收官 hard 题（g-02/g-03）亲自深分析 10-30 分钟（"你的时间是调度"边界漂移），期间 4 题执行者空转、并行机闲置 ~45 分钟（stall 告警正确触发）。
- 但**深分析产出决定性 prompt**：g-03 HTTP desync 一击命中、g-02 主 agent 亲自兜底参数爆破成功。
- 权衡定论：sharp-prompt 打法对 hard 题价值真实；问题是串行——**改进方向：fanout 先并行征思路（2 分钟 5 模型）替代主 agent 单线程深挖；或给分析设预算后强制先派一版执行者**。

## 三、机制实证（哪些设计被验证有效）
1. 集思账本：12 首批派单 9 个按 9/9 胜率选 deepseek-v4-flash；glm 3 连败后主 agent 主动弃用（负反馈闭环）。
2. CTF 公理：g-39 附件先取解出；全 run 无占位 flag 误交。
3. 派单可靠性：38+ verdict，failed 仅 glm×3（pre-fix），round-timeout 0 触发。
4. 花费纪律：hint 0 使用；贵模型仅 fanout 一次；主力夜间半价。
5. 进程边界设计：执行者独立子进程，主 agent 被 kill 不影响在途交卷；goal-rearm 无损续跑。
6. 告警面：stall/runner-restart 告警均按设计触发，实战准确。

## 四、战后已落实/待办
- 已落实：F8 护栏（jisi+runner）、F10/F15/F16/F17（jintuo/runner）、profile 热装新构建、现场记录归档。
- 待办：
  1. **deepseek-native-adapter usage sidecar**（F9，成本计量闭环的关键缺口）。
  2. **hufu campaign 状态序列化**（F18，恢复成本从"重推状态"降到"续跑"）。
  3. **F13 复盘**：continuable 的定位——保留机制但改进发现性（工具描述/SKILL 明示"硬题磨题用 continuable 而非重派"）。
  4. **F19 改进**：SKILL 加"分析预算 + fanout 先行"；或 runner 加"主 agent 深挖提醒"（不强制，只提示）。
  5. **boards 按 run 隔离 + 开跑前自动清场脚本**（F5/F6 根治）。
  6. 启动规程脚本化进 jintuo README（F1-F4 根治）。
  7. Kimi/DeepSeek 余额快照已校准；智谱 ¥156.09。

## 附录：9/8 生产凭据污染事故（F1 的下游后果，hermes 调查核对结论）

**事实链（本会话亲眼证据核对，全部属实）**：
- 01:49:36 我首次启动战役时只导出 GUARD_DSH_HOME 未导出 DSH_HOME（F1）→ dev 新版 CLI 默认落 home ~/.dsh（生产）→ 01:50 在 ~/.dsh/sessions/ 建了 --home-zrn-xiaochang-work-- 会话，并把 ~/.dsh/.credentials.yaml 从扁平格式覆写为 {version:1, refs:{...}}（本会话 01:50 实测：73B）。
- 生产 checkout 的 credentials-local 文档明示：**格式 = 纯扁平映射，无 version、无包装层；非字符串值一律拒绝**——refs 包装正是 web 启动 "must be a string" 崩溃的来源。
- 处置（已完成）：①凭证文件已恢复扁平格式（09:51，54B，含正确 key；python 校验 schema OK）；②生产 home 残留会话与 session_projcache（01:50 误建）已移入 xiaochang-archive/prod-home-pollution/；③run6-launch.sh 增加 home 隔离硬校验（DSH_HOME≠~/.dsh-dev 直接 FATAL 退出，已测 exit=2）；④误装到生产 home 的 headless profile 目录此前已清。
- 待用户：重启生产 web 验证（当前实例 530 仍在内存凭据上运行，勿动）；wsldiag 侧 daemon 日志（Windows 快捷方式侧）无法从本会话核实，以 hermes 记录为准。

**防复发（已写进启动器 + 应进 jintuo README）**：
```bash
# 战役启动器第一段：home 隔离硬校验
[ "${DSH_HOME:-}" = "/home/zrn/.dsh-dev" ] || { echo "FATAL: DSH_HOME must be /home/zrn/.dsh-dev"; exit 2; }
```

## 附录二：两套 DSH 环境的配置级隔离（2026-09-08，用户要求"靠配置不靠纪律"）

**"瞬间又崩"真相（与凭据无关）**：web.log 显示 10:28:07 web 退出 code=129（SIGHUP）@45s——WSL 发行版 10:26:26/10:26:47 连续两次整体重启（dmesg "Init has exited. Terminating distribution"），重启后首代 web 所在进程组随启动它的 wsl 会话关闭收到 SIGHUP（node 把继承的 SIGHUP 忽略重置为默认）；nohup 的 daemon 存活并把 gen-2 拉起，浏览器只看到 ~20s 空隙。此后 web 稳定（10:38 实测 200/62ms）。

**配置级隔离（已落地，全部在配置/代码层，无纪律依赖）**：
1. **dev checkout 默认 home 改为 ~/.dsh-dev**（`packages/util/home-paths/lib/index.js` + types 同步；补丁存 shence-jintuo/patches/dev-default-home.patch，check.sh 增加第 4 项检查，已验：defaultDshHome() → /home/zrn/.dsh-dev，显式 DSH_HOME 仍可覆盖）。裸跑 dev CLI 从任何地方都不会再落 ~/.dsh。
2. **生产 web daemon 反向钉死 home**（dsh-daemon.sh：`export DSH_HOME="${HOME}/.dsh"`，免疫任何全局 env）+ **web 子进程 setsid**（根治 129@45s 的 SIGHUP 闪断）。下次重启 web 生效。
3. run6-launch.sh 已有 DSH_HOME 硬校验（FATAL 退出），三重保险。
4. 生产 checkout 的默认 home 保持 ~/.dsh 未动（已核）。

**注意**：生产 daemon 脚本改动在下次 web 重启时生效；dev checkout 若被上游重建需重打补丁（check.sh 第 4 项会提示）。
