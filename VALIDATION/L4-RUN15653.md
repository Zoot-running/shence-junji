# L4 验证报告（第五场）：run 15653 —— v2 满贯（40/40）

> 2026-09-07 01:48 创建，05:45 结束（elapsed 14150s ≈ 3.93h，主 agent 提前收工并停表）。

## 最终成绩（平台口径）

- **40/40 题，20800/20800 分（final_score 100.0）**——与老架构（run 13250）满分对齐，
  且用时更短（3.93h vs 6h 跑满），全程主 agent 掌控、goal 轮驱动、零人工干预。

## 五场完整曲线（神策从 v1 到 v2 的实证迭代）

| run | 架构 | 结果 | 关键教训 |
|---|---|---|---|
| 13250（老） | 单 agent 连续调度 + continuable 子代理 + 本地经验库 | 40/40 | 机制基准（已提取归档） |
| 15309 | v1.2 自主 runner 首飞 | 31/40 | 13 项缺陷 |
| 15451 | v1.2 修复 | 37/40 | 89.9% |
| 15515 | v2 主 agent 调度首飞 | 36/40 | 10 项行为/架构缺陷 |
| 15640 | v2（guard 启动风暴事故） | 8/40 弃 | guard 误杀根因 → 修复 |
| **15653** | **v2 全部修复 + 老机制三教训回植** | **40/40** | **满贯** |

## 本场新发现（赛后）

1. **主 agent 模型被 settings.yaml 静默覆盖**：dev 的 `settings.yaml` 里
   `agent-default-model: kimi-k2.6` 压过了 profile 补丁的 deepseek-v4-pro——
   **三场 v2 战役的主 agent 其实一直是 kimi-k2.6**。这解释了 Kimi 烧钱速度
   （1269 次 k2.6 调用 = 主 agent 自身的轮次），也解释了它的风格。已把 settings
   改为 deepseek-v4-pro/max。教训：**改默认模型要看 settings.yaml 与 profile 的
   优先级，别只改补丁**。
2. 满贯之下仍有可改进：g-18/g-28 等难题靠多轮重派 + 主 agent 手攻磨出；
   continuable 执行者（保留上下文续战）会让这部分更顺。

## 花费账（run 5 实测）

- Kimi ¥213→181（约 ¥32；大头其实是**主 agent 自己的 k2.6 轮次**——settings 修正后
  主 agent 转 deepseek，kimi 消耗预期大降）；
- DeepSeek ¥364→305（约 ¥59）；
- 智谱 glm 全家桶共 ~180 次调用（少量）；
- 用量 sidecar 完整记录了 per-model token（供 jisi_usage 计价）。

## 遗留（按优先级，run 6 听指挥）

1. **单价校准**：登录 Kimi/智谱平台账单（等用户给短信验证码），把 jisi 单价表从
   估算改实账；
2. **continuable 执行者**（jisi 后台模式）：老机制最强一块，难题磨题连续性正解；
3. **花费闸门**：run 级成本上限 + 主 agent 每轮 jisi_usage 决策（机制已备，纪律待验证）；
4. **主 agent 模型回归验证**：settings 修正后，下一场主 agent = deepseek-v4-pro，
   需观察决策质量与速度变化；
5. 生产实例迁移（四插件 + 升级）——等用户指示；
6. 轮次超时差异化（easy 20/medium 40/hard 60 分钟）已在 66751bc 落地，下场生效。
