# L4 验证档案:Tsecbench v1 首战(run 18320,v22 镜像,集思 v2 决策内核)

- 时间:2026-09-14 23:36 → 05:42(打满 6h 钟)
- 结果:**final 82.3,18470/23000(80.3%)**;榜一门槛 Cairn_Y 97.14 → **未上榜一,未发布**(按用户规则)
- XBOW 复打:收官 05:42,距 9 点 3.3h < 5h → **按规则跳过**

## 一、v2 机制验证(今天改动的实测)

| 机制 | 实测 | 判定 |
|---|---|---|
| jisi_pick 契合度 | **537 次调用** | ✅ 大规模使用 |
| jisi_adjudicate 裁决 | **693 次调用** | ✅ 大规模使用 |
| v2 账本种子迁移 | model_report 显示 flash 0.98 n=64 / glm 0.90 n=19 | ✅(18293 的冷启动 bug 修复生效) |
| Thompson 探索 | pro 首场大规模上场(1357 调用)+ 9 模型目录全被 pick 过 | ✅ 冷启动死循环破除 |
| xiaochang_refanout(R2) | **0 次** | ❌ 升级链从未触发(根因见 §三-1) |
| report.why 归因 | **0 条** | ❌ 纪律未被执行(根因见 §三-1) |
| report.gaps 缺口 | 4 条 | 弱使用 |

## 二、战局轨迹

- 40min 43%、70min 68.7% —— 开局极快(18293 同帧才 3.5%);
- 然后撞上 **b-02(hard 1800 分、6 flag 多阶段 web)的硬墙**:74% 处磨了 ~3 小时,
  收尾钟前才破出 +1410 分(80.3% 收官);
- 多 flag 题(b-02 6 flags)按"一题一兵"打,阶段性 flag 没有独立工作项——尾部效率的根因候选。

## 三、明日分析要点

1. **升级链的断点:why 纪律依赖链**。⚠️升级建议 = 死思路/采纳 ≥50%,而死思路只在
   `report.why=approach-dead-end` 的终局对账里计数——本局 why=0 → deadIdeas 恒 0 →
   ⚠️ 永不出现 → refanout 永不调用。**机制修正方向**:status 的升级建议改用"该题终态失败次数"
   作死思路代理(不依赖归因字段);末段赶工 flag 同理,出现时应直接给出一键 refanout 的调用建议。
2. **末段赶工 flag 未转化为行动**:最后 60min 窗口内 agent 仍在磨 b-02 的 flash 路线,
   没有上 kimi/pro 换视角。纪律需要机制化——设计文档第 3 层的"默认动作"应真正默认:
   runner 在 flag 条件成立时主动触发 refanout(机制触发,agent 可改判),而不是只打提示。
3. **pro 首次大考**:1357 调用、2.27M 输出——v2 账本已攒出 pro 的真实数据,
   明日可看它的 execution 维度后验(T1 局它到底帮上了没)。
4. **花费**(周日谷价口径):flash ≈¥36 + pro ≈¥38 + kimi/glm ≈¥15 → 经济账 ≈¥90;
   钱包实扣待结算(DS 大概率平台承担, kimi/glm 小额)。
5. **多 flag 题**:T1 的 b-02(6 flags)一类题应按 flag 拆工作项(hufu item = flag 级),
   与"满分"口径对齐——画像/派单/账本都要按 flag 粒度。

## 四、今日已修(本局实锤的两处)

1. v2 账本初始化在种子落盘之前 → 沙箱首启丢 F33 种子(18293 冷启动)→ 已修+seed 文件迁移;
2. 托管开战令缺 v2 纪律 → 18293 的 0 裁决/0 why → 已补五条纪律(本局裁决 693 次验证生效)。
