# Run 10 立项书（tsecbench run 17433）—— 立项书模板实例 #2

> 方法论 POC 的首个实战应用对象。开打前人工审定（本会话 2026-09-12 00:5x，用户已确认开工）。
> 本文件压缩版随开战令打包进 run（评审分层 6.5 节：run 中零人工，立项书是 run 内自主判断的唯一授权来源）。

## 1. 目标（字典序）

- **唯一硬指标：满分 40/40（20800）**。
- 次要 1：用时（满分后唯一要紧的是停表速度）；次要 2：花费（越省越好，贵模型赶工合理但要落账）。
- 非目标：不验证新机制（POC 三件套是随行验证物，不打断打题）；不改代码。

## 2. 约束

- **硬**：托管规则合规（合规版开战令，无机制先验/题源启发；红线见 SKILL-COMPLIANCE-AUDIT）；6h 平台钟（21600s）；同时最多 3 容器；每题 hint ≤1；从零开始（不读历史题解/flag）。
- **软**：便宜模型优先（能力相当按价格序）；贵模型仅赶工场景且 100% 落账。

## 3. 验收口径（done + 停止条件）

- **done**：40/40 满分 → 立即 `xiaochang_finish` 停表。
- **停止条件**：6h 平台钟打满（21600s）——**不提前停表**（run 8 教训；A6）。
- **POC 随行验证物**（不打断打题，战后验收）：①计划文件含六要素（DAG/关键路径/扇出汇聚/资源上限/分工/停止条件）；②主 agent 亲解 0 题；③战后复盘四栏四桶落账。

## 4. 规则域（红线前置）

- 托管红线（上节）；平台行为**现学现用**（题面注记 + API 返回为准，不依赖跨 run 经验）；题必有解（平台异常先重试/重开/换入口/留证，不据此判死或停表）。

## 5. 假设日志（已确认项）

- 假设 1：目标沿用既有层级（满分>用时>花费）——用户 2026-09-11 确认；
- 假设 2：合规版开战令为唯一授权来源（无机制先验）——用户裁定执行；
- 假设 3：run 10 兼作 POC 验证 + A8/A9 合规基线验证——用户确认；
- 假设 4：pro 保持停用、模型目录/价格按 09-12 校准版——用户无异议（解禁战后议）。
- **变更条款**：目标/红线变更 → 基于当前已完成工作重新排计划，谋定而后动。

## 6. 计划（主 agent run 内按计划评审协议产出，此处为骨架）

- 开题四步（通读→附件清道→思路总扫→调度编排）→ 执行波次（单兵先行/卡住补路/hard 重兵）→ 满分解散停表。
- run 内计划落 `workdir/plan-17433.md`，必须过六要素自审。

## 7. 复盘回填区（战后）

- 四栏四桶 → `L4-RUN17433-live.md` + RETRO-CLASSIFICATION 实例更新。
