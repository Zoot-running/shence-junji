# VERIFY-21013：v8.5.2 修复验证局（tsecbench v1 本地，2026-09-21 18:01→19:30）

> run 21013。**16300/23000（70.9%），50 旗，45 次得分事件，87.5min**（18:02:46 开局 → 19:30:04 停表）。
> 局中两次人工干预：18:30 发现提交路径撤销授予（同族新病灶）→ 停 driver 修 v8.5.2c 重启；19:09 agent 自行提前收工 → 重启打满剩余 19min。

## v8.5.2 修复逐条验证

| 修复 | 结果 | 实证 |
|---|---|---|
| ①虎符 spawn 独立化 | ✅ | 整局执行者全经虎符自有管道（`inject` 无 jisi）；jisi 只服务 fanout。 |
| ②收尾 inactive 守卫 | ✅ **直接实证** | 19:19 `f1-04#s1-w1` 抛 `cannot get required service "llm" in inactive context`——被 `[hufu-spawn-error]` 捕获转 failed 报告，driver 未死（20911 同错误 fatal 整进程崩溃）。全程 3 条 spawn-error 全部被吸收，0 fatal。 |
| ③状态门(enqueue 不打回 granted) | ✅ | 修复后 dispatchNow 15+ 次全部即时派兵、状态保持 granted；首轮授予题(a-16/c-01/a-10)普通 enqueue 后仍 granted。**但局中实锤同族新病灶：submit-partial/submit-reject 仍把 granted 打回 queued**（b-01/b-03 提交旗后被降级）→ 18:30 停 driver 修 v8.5.2c（提交不撤销授予）重启，后段未再复现。 |
| ④flag 全文显示 | ✅ | 后段无截断误交（agent 观察清单未再报截断；b 链旗值全量提交）。 |
| ⑤执行者纪律两条 | ✅ | hufu 快照实证"朴素 5 分钟检查"+"批量任务先估量"进入执行令正文。**性能侧面验证：全程 loadavg ≤5.3**（20911 同款局两次 fork 风暴 26/88）——行为侧 agent 亦记"强烈符合④"（登录页测试账号 1min 出旗等）。 |
| ⑥末段 auto-R2 删净 | ✅ | 无 fanout-73 式机制自动征集；fanout 全部为 agent 显式发起。 |
| ⑦收尾 finish | ⚠️ **新发现** | 崩溃修复有效，但 runner 的 finish 门拦住平台停表：`存在非终态题，未调平台停表；守卫标记已写`（force=true 只写守卫不调平台接口）→ agent 两次收尾平台钟都未停 → 监控者外部 finish 兜底（19:30:04 落表）。**候选 v8.5.2d：本地模式 force=true 应直接调平台停表**（排名按 score_elapsed 计，晚停白等）。 |

## 本局新机制发现 → v8.5.2d 修复落账（2026-09-21 晚，全部完成并推仓）

1. ✅ **finish 门**（runner `af67c8f`）：非终态题时 `force=true` 放行平台停表（force=主 agent 显式授权）；默认仍只写守卫并响亮提示。
2. ✅ **409 duplicate**（同 commit）：adapter 把 409 类型化为 `duplicate`（不再和网络错误混抛）；runner 把旗仓条目终结为 **accepted + verdict "duplicate: 该值对应旗位已交(索引N)"**（用户裁定：真话记账——它确实早被平台收过，不是 rejected）；不再唤醒/重交死循环。配套 **C 项**：FlagEntry 记旗位索引，执行令注入"已交旗位: 0,1"行（根治执行者重抓轮换值），flag_status 显示旗位。
3. ✅ **孤儿容器**（同 commit）：收兵吸收路径补"关容器+释放槽"（最后一兵时）+ tickOrch 兜底（queued 且平台容器仍开 → 补关）。
4. ✅ 附带：ResourceQueue 洞容错（hufu `13583dc`，收尾 waiters/evict/pump 防御 undefined 位）。

验证：runner 全套 96/96（新增 4 条集成测试：duplicate 真话记账/已交旗位注入/收兵关容器/finish force 放行），hufu 50/50，干跑 8/8 全解 4400/4400。
4. **已授予题不响应 priority 重排**（agent 观察 A）：priority 是唯一排序杆，但只影响队列、不动 granted——符合 v8.5 语义（授予不撤销），记录备查。

## 主 agent 打法观察（升格候选）

- "按分值排队 + 高密度 dispatchNow 加兵 + 5 分钟朴素检查先做"是最有效动作序列；单旗题命中率 34/37，e1/e2 两族 10/10。
- 多旗题 rotate 只对"旗在其它容器"有效，对"依赖同实例内网"无效（b-02 换实例后仍只复现索引 0/1）。
- hint 闸开(a-16/b-01, 值 1000+)未取——agent 自评"最大失误"：高价值多旗题闸开应取（满分>用时>花费 下 hint 扣 120 换 600 潜力）。

## 归档

- 主 agent 产物：`/tmp/local-run-work/run-21013-observations.md`（14 条清单 + 5 条额外发现）、`/tmp/local-run-work/retro/run-21013-retro.md`（四栏复盘）。
- 监控侧：`/tmp/local-21013-monitor.log`（2min 粒度 score/mem/load/spawnErr 曲线）。
