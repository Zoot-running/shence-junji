# Run 11 立项书（tsecbench run 17497）—— 立项书模板实例 #3（行营模式首战）

> 方法论 POC W5 验收 + 行营模式（DSH_PRESET=xingying）首次实战。
> 开打前人工审定（2026-09-12 11:1x，用户已确认开工）。

## 1. 目标（字典序）

- **唯一硬指标：满分 40/40（20800）**。
- 次要 1：用时（满分后停表速度）；次要 2：花费。
- 非目标：不验证新模式机制以外的框架项；不改代码。

## 2. 约束

- **硬**：托管规则合规（合规版开战令，无机制先验）；6h 平台钟；≤3 容器；hint ≤1；从零开始。
- **软**：便宜模型优先；贵模型仅赶工且 100% 落账。

## 3. 验收口径（done + 停止条件 + 行营模式验收）

- **done**：40/40 → 立即 finish。
- **停止条件**：打满 6h——不提前停表。
- **行营模式验收（W5）**：①persona 三件套纪律在战役中可见（agent 引用/行为体现）；
  ②计划文件过 `xingying_plan_review` 六要素校验（或等价自审）；③战后 `xingying_retro` 四栏落账；
  ④jisi/hufu 主机服务在预设会话可用（软依赖验证）。

## 4. 规则域（红线前置）

- 托管红线；平台行为现学现用；题必有解、平台异常≠题无解（先排查留证，不据此判死/停表）。

## 5. 假设日志（已确认）

- 假设 1：目标层级沿用（满分>用时>花费）——用户确认；
- 假设 2：合规版开战令 + 行营模式 persona 为唯一授权来源——用户确认；
- 假设 3：run 11 兼作行营 W5 验收——用户确认（"开搞吧"）。
- **变更条款**：目标/红线变更 → 基于当前已完成工作重新排计划。

## 6. 计划

- 开题四步（通读→附件清道→思路总扫→调度编排）→ 单兵先行波次 → 满分解散停表；
- run 内计划落 `workdir/plan-17497.md`，派活前过六要素自审（`xingying_plan_review`）。

## 7. 复盘回填区（战后）
